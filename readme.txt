Given more time, I would store csv data into a database (probably mongo or postgres) so i wouldn't need to reload the csv every single page load. I did the geojson code in a pretty hacky way since i don't know python too well -- would look into libraries for cleaner solutions. 


